diff --git a/files/etc/hotplug.d/iface/15-mwan3 b/files/etc/hotplug.d/iface/15-mwan3
index 2ac5525..a220b30 100644
--- a/files/etc/hotplug.d/iface/15-mwan3
+++ b/files/etc/hotplug.d/iface/15-mwan3
@@ -68,6 +68,8 @@ case "$ACTION" in
 		mwan3_create_iface_iptables $INTERFACE $DEVICE
 		mwan3_create_iface_rules $INTERFACE $DEVICE
 		mwan3_create_iface_route $INTERFACE $DEVICE
+		# Remove any stale recovery rule - normal fwmark routing takes over
+		mwan3_del_recovery_rule $INTERFACE
 		if [ "${status}" = "online" ]; then
 			$LOG notice "Starting tracker on interface $INTERFACE (${DEVICE:-unknown})"
 			mwan3_set_iface_hotplug_state $INTERFACE "online"
diff --git a/files/etc/hotplug.d/iface/16-mwan3 b/files/etc/hotplug.d/iface/16-mwan3
index dd09358..64e7c4d 100644
--- a/files/etc/hotplug.d/iface/16-mwan3
+++ b/files/etc/hotplug.d/iface/16-mwan3
@@ -20,6 +20,23 @@ fi
 config_get_bool enabled "$INTERFACE" 'enabled' '0'
 [ "${enabled}" -eq 0 ] || {
 	mwan3_flush_conntrack "$INTERFACE" "$ACTION"
+
+	# Manage src-IP recovery rule so mwan3track pings can reach
+	# targets via the interface's routing table during recovery.
+	case "$ACTION" in
+		disconnected)
+			# Interface just went offline - add recovery rule so
+			# mwan3track pings bypass the unreachable policy mark
+			mwan3_init
+			mwan3_set_recovery_rule "$INTERFACE"
+		;;
+		connected)
+			# Interface back online - remove recovery rule, normal
+			# fwmark policy routing takes over
+			mwan3_init
+			mwan3_del_recovery_rule "$INTERFACE"
+		;;
+	esac
 }
 
 mwan3_unlock "$ACTION" "mwan3rtmon"
diff --git a/files/lib/mwan3/mwan3.sh b/files/lib/mwan3/mwan3.sh
index 39fc605..ec20bfc 100644
--- a/files/lib/mwan3/mwan3.sh
+++ b/files/lib/mwan3/mwan3.sh
@@ -632,6 +632,65 @@ mwan3_delete_iface_rules()
 	fi
 }
 
+mwan3_set_recovery_rule()
+{
+	# Add a src-IP based ip rule so mwan3track recovery pings bypass
+	# the mangle policy chain (which marks them MMX_UNREACHABLE when all
+	# members are offline) and route directly via the interface's table.
+	#
+	# Priority id+1500 sits between iif rules (id+1000) and fwmark rules
+	# (id+2000), before the blackhole/unreachable rules (2061/2062).
+	# This means unmarked recovery pings sourced from the interface IP
+	# are caught here and routed correctly without touching the policy chain.
+	#
+	# SRC_IP is written by mwan3track on startup to its state dir.
+	local iface="$1"
+	local id src_ip family
+
+	config_get family "$iface" family ipv4
+	mwan3_get_iface_id id "$iface"
+	[ -n "$id" ] || return 0
+
+	src_ip=$(cat "$MWAN3TRACK_STATUS_DIR/$iface/SRC_IP" 2>/dev/null)
+	[ -n "$src_ip" ] && [ "$src_ip" != "0.0.0.0" ] && [ "$src_ip" != "::" ] || return 0
+
+	if [ "$family" = "ipv4" ]; then
+		while [ -n "$($IP4 rule list | awk '$1 == \"'$(($id+1500)):'\"')" ]; do
+			$IP4 rule del pref $(($id+1500))
+		done
+		$IP4 rule add pref $(($id+1500)) from "$src_ip" lookup "$id"
+		$LOG info "Recovery rule added: from $src_ip lookup $id for interface $iface"
+	elif [ "$family" = "ipv6" ] && [ $NO_IPV6 -eq 0 ]; then
+		while [ -n "$($IP6 rule list | awk '$1 == \"'$(($id+1500)):'\"')" ]; do
+			$IP6 rule del pref $(($id+1500))
+		done
+		$IP6 rule add pref $(($id+1500)) from "$src_ip" lookup "$id"
+		$LOG info "Recovery rule added: from $src_ip lookup $id for interface $iface"
+	fi
+}
+
+mwan3_del_recovery_rule()
+{
+	# Remove the src-IP recovery rule when interface comes back online
+	# or is fully torn down.
+	local iface="$1"
+	local id family
+
+	config_get family "$iface" family ipv4
+	mwan3_get_iface_id id "$iface"
+	[ -n "$id" ] || return 0
+
+	if [ "$family" = "ipv4" ]; then
+		while [ -n "$($IP4 rule list | awk '$1 == \"'$(($id+1500)):'\"')" ]; do
+			$IP4 rule del pref $(($id+1500))
+		done
+	elif [ "$family" = "ipv6" ] && [ $NO_IPV6 -eq 0 ]; then
+		while [ -n "$($IP6 rule list | awk '$1 == \"'$(($id+1500)):'\"')" ]; do
+			$IP6 rule del pref $(($id+1500))
+		done
+	fi
+}
+
 mwan3_delete_iface_ipset_entries()
 {
 	local id setname entry
diff --git a/files/usr/sbin/mwan3track b/files/usr/sbin/mwan3track
index cd4dc3b..b3c99df 100755
--- a/files/usr/sbin/mwan3track
+++ b/files/usr/sbin/mwan3track
@@ -71,6 +71,7 @@ main() {
 	STATUS=$3
 	SRC_IP=$4
 	mkdir -p /var/run/mwan3track/$1
+	echo "$SRC_IP" > /var/run/mwan3track/$1/SRC_IP
 	trap clean_up TERM
 	trap if_down USR1
 
